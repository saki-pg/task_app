<h3>スケジュール詳細</h3>
 
 <table>
   <tr>
     <th>タイトル</th>
     <td><%= @post.title %></td>
   </tr>
   <tr>
     <th>開始日</th>
     <td><%= @post.start.to_s(:date_jp) %></td>
   </tr>
   <tr>
     <th>投稿日時</th>
     <td><%= @post.created_at.to_s(:datetime_jp) %></td>
   </tr>
   <tr>
     <th>終日</th>
     <td><% if @post.all_day == true %>◯
         <% else @post.all_day == false %>
         <% end %></td>
   </tr>
   <tr>
     <th>スケジュールメモ</th>
     <td><%= @post.introduction %></td>
   </tr>
 </table>
 
 <div>
   <ul>
     <li><%= link_to "スケジュール一覧に戻る", :posts %></li>
   </ul>
 </div>
 
<footer>
  <h2>現在の日時 : <%= Date.current.strftime("%Y年 %m月 %d日") %>
  </h2> 
</footer>